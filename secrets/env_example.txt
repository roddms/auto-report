# GPT 방식의 환경 변수 설정 파일
# 실제 DB 연결 정보를 여기에 입력하세요

# 데이터베이스 종류 (mysql, postgresql, mssql)
DB_KIND=mysql

# 데이터베이스 연결 정보
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_username
DB_PASS=your_password
DB_NAME=your_database

# 로그 레벨
LOG_LEVEL=INFO

